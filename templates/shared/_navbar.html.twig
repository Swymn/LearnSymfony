<nav class="navbar navbar-expand-lg navbar-light bg-light p-2">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">E-Boutique</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor03">
            <ul class="navbar-nav me-auto">
                {% for category in categories.findAll() %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_category_show', {'slug': category.slug}) }}">{{ category.name }}</a>
                    </li>
                {% endfor %}
            </ul>
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_cart') }}">
                        <i class="fas fa-cart-shopping"></i>
                        <span class="badge rounded-pill bg-info">{{ cartService.cartSize() }}</span>
                        Pannier ({{ (cartService.getTotal() / 100) | format_currency('EUR', locale= 'fr')}})
                    </a>
                </li>
            </ul>
            <ul class="navbar-nav mx-4">
                {% if not app.user %}
                    <li class="nav-item">
                        <a class="nav-link mx-1" href="#">Inscription</a>
                    </li>
                    <li class="nav-item mx-1">
                        <a class="nav-link btn btn-second" href="{{ path('app_auth_login') }}">Connexion</a>
                    </li>
                {% else %}
                    <li class="nav-item mx-1">
                        <a class="nav-link btn btn-second" href="{{ path('app_purchase_list') }}">Mes Commandes</a>
                    </li>
                    <li class="nav-item mx-1">
                        <a class="nav-link btn btn-second" href="{{ path('app_auth_logout') }}">Déconnexion</a>
                    </li>
                {% endif %}
            </ul>
            <form class="d-flex" action="{{ path('app_home_search') }}">
                <input class="form-control me-sm-2" type="text" placeholder="Search" name="value">
                <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
            </form>
        </div>
    </div>
</nav>