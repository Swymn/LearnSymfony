<div class="card">
    <img src="{{ card.picture }}" class="img-fluid" alt="Product Image">
    <div class="card-body">
        <h4 class="card-title">{{ card.name }}</h4>
        <h6 class="card.subtitle">{{ card.convertPrice | format_currency('EUR', locale='fr')}}</h6>

        <span class="badge bg-success">
            {{ card.category.name }}
        </span>

        <p class="card-desc">{{ card.shortDescription }}</p>
        <a href="{{ path('product_show', {'category_slug': card.category.slug, 'product_slug': card.slug })}}" class="btn btn-second btn-sm">Détails</a>
        <a href="#" class="btn btn-outline-success btn-sm">Ajouter</a>
    </div>
</div>